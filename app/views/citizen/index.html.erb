<% if @citizens.any? %>
  <h3 class="title"> Munícipes Cadastrados </h3>

  <%= form_tag(citizend_path, method: "get") do %>
    <%= text_field_tag :name, params[:name], placeholder: "Pesquisar por nome" %>
    <%= submit_tag "Pesquisar", id: "search-button", class: "btn btn-default btn-success" %>
  <% end %>

  <table class="table table-striped table-bordered table-hover">
    <thead>
    <tr>
      <th><%= I18n.t("activerecord.attributes.municipe.name")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.email")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.cpf")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.cns")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.birthdate")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.phone")  %></th>
      <th><%= I18n.t("activerecord.attributes.municipe.status.one")  %></th>
      <th><%= I18n.t("titles.actions.one")  %></th>
    </tr>
    </thead>

    <tbody>
    <% @citizens.each do |citizen| %>
      <tr>
        <td><%= citizen.name %></td>
        <td><%= citizen.email %></td>
        <td><%= citizen.cpf %></td>
        <td><%= citizen.cns %></td>
        <td><%= citizen.birthdate %></td>
        <td><%= citizen.phone %></td>
        <td><%= citizen.status %></td>
        <td><%= link_to I18n.t('titles.actions.edit'), edit_municipe_path(municipe), class: "btn btn-default btn-secondary" %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
<% end %>